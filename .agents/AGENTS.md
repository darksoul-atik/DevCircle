# Rules

- **Git Workflow**: After making any significant code changes or completing a requested feature, always commit the changes (`git add .`, `git commit`) and push them to the remote GitHub repository (`git push origin main` or the current active branch). Do not wait for the user to explicitly ask to push.
